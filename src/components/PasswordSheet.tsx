import * as React from "react";
import {Transaction} from "../types";

interface Props{
    tx:Transaction
}

class PasswordSheet extends React.Component<any, Props>{

    render() {
        return undefined;
    }
}
